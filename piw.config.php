<?
### CONFIGURATION FILE

// swapdir: directory to store swap (cache) files (can be "." to take the current directory)
$swapdir = "./cache";

// output: image format (jpeg, gif and png)
$output = "jpeg";

// jpeg quality: image quality (in case the above var is set to jpeg): minimum, low, medium, high, maximum (the lower the quality, the lower the file size).
$jpegquality = "high";

// stretch: whether or not stretch images on resampling.
$stretch = false;

// no swap mode: mode without swap (cache) file
$noswapmode = false;

?>